"""
Test suite for data-orchestration-airflow.

Covers DAG structural validation. Runtime and Docker integration
tests are intentionally excluded — those are validated via
`docker compose up` and manual Airflow UI inspection.
"""
