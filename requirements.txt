# =============================================================================
# data-orchestration-airflow — Python Requirements
# =============================================================================
#
# PURPOSE
# -------
# This file serves two distinct uses:
#
#   1. LOCAL DEVELOPMENT & TESTING (outside Docker):
#      Install these packages in a virtualenv to run `pytest` locally.
#      Note: apache-airflow must also be installed separately (see below).
#
#   2. DOCKER CONTAINER ADDITIONS:
#      The `_PIP_ADDITIONAL_REQUIREMENTS` variable in docker-compose.yml
#      (if used) references this file. The base Airflow image already
#      provides apache-airflow and its core dependencies; this file adds
#      packages beyond what the base image includes.
#
# INSTALLING apache-airflow LOCALLY FOR TESTS
# --------------------------------------------
# The official method uses a constraint file to prevent dependency conflicts:
#
#   pip install apache-airflow==2.10.2 \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.10.2/constraints-3.12.txt"
#   pip install -r requirements.txt
#
# =============================================================================

# -----------------------------------------------------------------------------
# Testing and Linting
# -----------------------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0

# -----------------------------------------------------------------------------
# ETL Integration Dependencies
# -----------------------------------------------------------------------------
# Required by economic-data-etl when mounted at /opt/airflow/etl/.
# pandas, requests, sqlalchemy, and psycopg2-binary are provided by the
# base Airflow image. python-dotenv is not and must be installed explicitly.
python-dotenv>=1.0.0
